# 📚 WorldMates Messenger - Довідник Функцій

**Дата:** 2026-01-10
**Версія:** 1.0

Цей файл містить технічну документацію про всі реалізовані та в процесі функції месенджера.

---

## 📖 ЗМІСТ

1. [Stories (Історії)](#stories)
2. [Channels (Канали)](#channels)
3. [Cloud Backup (Хмарний Бекап)](#cloud-backup)
4. [Emoji & Stickers (Емодзі та Стікери)](#emoji-stickers)
5. [GIF підтримка](#gif)
6. [Maps (Карти та Локація)](#maps)

---

## <a name="stories"></a>📱 1. STORIES (Історії)

### Статус: ✅ Реалізовано (UI + API + Socket.IO)

### Компоненти:
- **Android UI:**
  - `PersonalStoriesRow.kt` - Горизонтальний ряд сторіс особистих контактів
  - `ChannelStoriesSection.kt` - Секція сторіс каналів
  - `StoryViewerActivity.kt` - Перегляд сторіс на весь екран
  - Інтеграція в `ChatsScreenModern.kt`

- **Моделі:**
  - `Story.kt` - Модель сторі з медіа, реакціями, переглядами
  - `StoryUser.kt` - Дані користувача
  - `StoryMedia.kt` - Медіа контент (зображення/відео)
  - `StoryReactions.kt` - Реакції на сторі

### API Endpoints:
```
GET /api/v2/endpoints/get-stories.php
POST /api/v2/endpoints/create-story.php
POST /api/v2/endpoints/delete-story.php
POST /api/v2/endpoints/view-story.php
POST /api/v2/endpoints/react-to-story.php
```

### Socket.IO Events:
```javascript
// Отримати нову сторі
socket.on('new_story', (data) => { ... });

// Сторі переглянуто
socket.on('story_viewed', (data) => { ... });

// Реакція на сторі
socket.on('story_reaction', (data) => { ... });
```

### Особливості:
- ✅ Підтримка зображень та відео
- ✅ Реакції емодзі на сторі
- ✅ Перегляди з аватарками користувачів
- ✅ Коментарі до сторіс
- ✅ Автопрогрес (зникають через 24 год)
- ✅ Індикатор переглянуто/не переглянуто (фіолетове обведення)

### Що треба доробити:
- ⚠️ Навігація між сторіс (зараз показується тільки остання сторі)
- ⚠️ Камера для створення сторі (поки тільки вибір з галереї)

---

## <a name="channels"></a>📢 2. CHANNELS (Канали)

### Статус: ⚠️ Частково реалізовано (UI + API, Socket.IO потребує міграції)

### Компоненти:
- **Android UI:**
  - `ChannelsTab.kt` - Список каналів
  - `ChannelCard.kt` - Картка каналу
  - `ChannelDetailScreen.kt` - Екран каналу з постами
  - Інтеграція в `ChatsScreenModern.kt`

- **Моделі:**
  - `Channel.kt` - Модель каналу
  - `ChannelPost.kt` - Пост в каналі
  - `ChannelMember.kt` - Учасник каналу

### API Endpoints:
```
GET /api/v2/endpoints/get-user-channels.php
POST /api/v2/endpoints/create-channel.php
POST /api/v2/endpoints/update-channel.php
POST /api/v2/endpoints/delete-channel.php
POST /api/v2/endpoints/join-channel.php
POST /api/v2/endpoints/leave-channel.php
POST /api/v2/endpoints/get-channel-posts.php
POST /api/v2/endpoints/create-channel-post.php
```

### Socket.IO Events (ПОТРЕБУЄ МІГРАЦІЇ):
```javascript
// ❌ Зараз: polling кожні 15 сек
// ✅ Треба: real-time Socket.IO

// Новий пост в каналі
socket.on('new_channel_post', (data) => { ... });

// Оновлення каналу
socket.on('channel_updated', (data) => { ... });
```

### Особливості:
- ✅ Створення/редагування/видалення каналів
- ✅ Вступ/вихід з каналів
- ✅ Публікація постів з медіа
- ✅ Аватарка та опис каналу
- ✅ Список учасників
- ❌ Real-time оновлення (зараз polling)

### Що треба доробити:
- 🔥 КРИТИЧНО: Міграція на Socket.IO (прибрати polling)
- ⚠️ Адмін панель для керування каналом
- ⚠️ Права доступу (admin, moderator, user)
- ⚠️ Пінування постів

---

## <a name="cloud-backup"></a>☁️ 3. CLOUD BACKUP (Хмарний Бекап)

### Статус: ⚠️ Реалізовано (UI + API, але статистика placeholder)

### Компоненти:
- **Android UI:**
  - `CloudBackupScreen.kt` - Екран налаштувань бекапу
  - `CloudProviderCard.kt` - Картка провайдера (Google Drive, Dropbox)
  - Інтеграція в `SettingsActivity.kt`

- **Моделі:**
  - `CloudBackup.kt` - Модель бекапу
  - `CloudProvider.kt` - Провайдер (Google, Dropbox, OneDrive)

### API Endpoints:
```
GET /api/v2/endpoints/get-cloud-backups.php
POST /api/v2/endpoints/create-backup.php
POST /api/v2/endpoints/restore-backup.php
POST /api/v2/endpoints/delete-backup.php
POST /api/v2/endpoints/connect-cloud-provider.php
```

### Особливості V1:
- ✅ Вибір провайдера (Google Drive, Dropbox, OneDrive)
- ✅ Ручний бекап
- ✅ Автоматичний бекап (щоденно/щотижня/щомісяця)
- ✅ Відновлення з бекапу

### Особливості V2:
- ✅ Шифрування бекапів (AES-256)
- ✅ Вибір що бекапити (повідомлення/медіа/налаштування)
- ✅ Історія бекапів
- ✅ Розмір бекапу

### Що треба доробити:
- 🔥 КРИТИЧНО: Реальна статистика бекапів (зараз placeholder)
- ⚠️ OAuth для Google Drive / Dropbox
- ⚠️ Прогрес бекапу в реальному часі
- ⚠️ Перевірка цілісності бекапу

---

## <a name="emoji-stickers"></a>😊 4. EMOJI & STICKERS (Емодзі та Стікери)

### Статус: ✅ Повністю реалізовано

### Компоненти:
- **Android UI:**
  - `EmojiPicker.kt` - Клавіатура емодзі (8 категорій, 400+ емодзі)
  - `StickerPicker.kt` - Панель стікерів
  - `CustomEmojiTab.kt` - Кастомні емодзі
  - Інтеграція в `MessagesScreen.kt`

- **Моделі:**
  - `CustomEmoji.kt` - Кастомне емодзі
  - `EmojiPack.kt` - Пакет емодзі
  - `Sticker.kt` - Стікер
  - `StickerPack.kt` - Пакет стікерів

### API Endpoints:
```
GET /api/v2/endpoints/get-emoji-packs.php
POST /api/v2/endpoints/activate-emoji-pack.php
POST /api/v2/endpoints/deactivate-emoji-pack.php
GET /api/v2/endpoints/get-sticker-packs.php
POST /api/v2/endpoints/activate-sticker-pack.php
POST /api/v2/endpoints/send-sticker.php
```

### Категорії Емодзі:
1. 😊 Смайлики та емоції
2. ✋ Жести та люди
3. 🐶 Тварини
4. 🍎 Їжа та напої
5. ⚽ Активності
6. ✈️ Подорожі
7. 💡 Об'єкти
8. ❤️ Символи

### Особливості:
- ✅ 400+ стандартних емодзі
- ✅ Кастомні емодзі (upload через Strapi CMS)
- ✅ 32 стандартних стікери
- ✅ Кастомні стікер-паки
- ✅ Відправка стікерів одним кліком
- ✅ Швидкі реакції емодзі на повідомлення (8 емодзі)

---

## <a name="gif"></a>🎬 5. GIF ПІДТРИМКА

### Статус: ✅ Реалізовано (Giphy API)

### Компоненти:
- **Android UI:**
  - `GifPicker.kt` - Пошук та вибір GIF
  - Інтеграція в `MessagesScreen.kt`

- **API:**
  - Giphy SDK (trending, search)
  - Endpoint: `https://api.giphy.com/v1/gifs/`

### Особливості:
- ✅ Пошук GIF за ключовим словом
- ✅ Trending GIF
- ✅ Попередній перегляд
- ✅ Відправка GIF в чат

### Що треба доробити:
- 🔥 КРИТИЧНО: Заміна demo API ключа на production
- ⚠️ Категорії GIF (смішні, привітання, реакції)
- ⚠️ Збереження улюблених GIF

---

## <a name="maps"></a>🗺️ 6. MAPS (Карти та Локація)

### Статус: ✅ Реалізовано (Google Maps)

### Компоненти:
- **Android UI:**
  - `LocationPicker.kt` - Вибір локації на карті
  - `LocationMessageBubble.kt` - Відображення локації в чаті
  - Інтеграція в `MessagesScreen.kt`

- **API:**
  - Google Maps SDK
  - Google Places API (пошук місць)

### Особливості:
- ✅ Відправка поточної локації
- ✅ Вибір локації на карті
- ✅ Відображення локації в чаті (інтерактивна карта)
- ✅ Відкриття в Google Maps
- ❌ Live Location (треба реалізувати)

### Що треба доробити:
- 🔥 КРИТИЧНО: Заміна demo API ключа на production
- ⚠️ Live Location (реал-тайм трекінг локації)
- ⚠️ Поділитися локацією на певний час (15хв/1год/8год)

---

## 🔄 ЗАГАЛЬНІ ПОКРАЩЕННЯ

### Socket.IO:
- ✅ Повідомлення - real-time
- ✅ Статус користувача - real-time
- ✅ Typing індикатор - real-time
- ✅ Stories - real-time
- ❌ Channels - поки polling (треба мігрувати)

### Шифрування:
- ✅ AES-256-GCM для нових повідомлень
- ⚠️ AES-128-ECB для старих (legacy, треба мігрувати)
- ❌ Секретні чати E2E (треба реалізувати)

### Медіа:
- ✅ Зображення (JPEG, PNG, WebP)
- ✅ Відео (MP4, WebM)
- ✅ Аудіо (MP3, AAC, OGG)
- ✅ Документи (PDF, DOC, XLS)
- ✅ GIF (Giphy)
- ❌ Voice Messages (треба реалізувати)

---

## 📊 СТАТИСТИКА РЕАЛІЗАЦІЇ

```
Повністю реалізовано:     ████████░░  80%
Потребує доробки:         ████░░░░░░  45%
Критичні завдання:        ██████░░░░  60%
```

### Пріоритети:
1. 🔥 Заміна demo API ключів (Giphy, Google Maps)
2. 🔥 Stories навігація fix
3. 🔥 Channels Socket.IO міграція
4. ⚠️ Cloud Backup статистика
5. ⚠️ Live Location
6. ⚠️ Voice Messages

---

**Дата створення:** 2026-01-10
**Останнє оновлення:** 2026-01-10
**Статус:** Актуальна документація
