# 🧪 ІНСТРУКЦІЯ ДЛЯ ТЕСТУВАННЯ

## 📱 ПЕРЕД ТЕСТУВАННЯМ:

### 1. Pull останні зміни:
```bash
git pull origin claude/fix-swipe-media-upload-Kx6yS
```

### 2. В Android Studio:
```
File → Invalidate Caches / Restart
Build → Clean Project
Build → Rebuild Project  # ВАЖЛИВО: саме Rebuild!
```

### 3. Перевір Build Output:
- Відкрий вкладку **Build** внизу
- Переконайся що **"BUILD SUCCESSFUL"**
- Якщо є помилки - скопіюй їх повністю!

### 4. Створи APK:
```
Build → Build Bundle(s) / APK(s) → Build APK(s)
```

### 5. Знайди APK:
```
app/build/outputs/apk/debug/app-debug.apk
```
**Перевір дату модифікації файлу** - має бути свіжа (сьогодні)!

### 6. ВИДАЛИ старий додаток:
- Повністю видали WorldMates Messenger з телефону
- Це КРИТИЧНО! Інакче старий код залишиться

### 7. Встанови новий APK:
```bash
# Через ADB:
adb install app/build/outputs/apk/debug/app-debug.apk

# Або перетягни APK на телефон і встанови вручну
```

## 🎯 ТЕСТУВАННЯ ФУНКЦІЙ:

### ТЕСТ 1: Довгий тап (Long Press)

1. Відкрий будь-який чат
2. **Довгий тап** (утримуй палець) на будь-якому повідомленні

**ЩО МАЄ СТАТИСЬ:**
- 🔔 Toast: "🎯 Довгий тап працює! Меню має відкритись!"
- 📋 З'явиться Bottom Sheet меню з опціями

**ЯКЩО НЕ ПРАЦЮЄ:**
- Меню НЕ відкривається → Проблема з combinedClickable
- Toast НЕ з'являється → Старий APK встановлений!

---

### ТЕСТ 2: Контекстне меню

Після довгого тапу має з'явитись меню:

**Для чужих повідомлень:**
- ↩️ Відповісти
- ➡️ Переслати  
- 📋 Копіювати текст (якщо є текст)

**Для СВОЇХ текстових повідомлень:**
- ↩️ Відповісти
- ✏️ **Редагувати** ← МАЄ БУТИ!
- ➡️ Переслати
- 📋 Копіювати текст
- 🗑️ **Видалити** ← МАЄ БУТИ!

**ЯКЩО НЕ З'ЯВЛЯЄТЬСЯ EDIT:**
- Перевір що це ТВОЄ повідомлення (синя бульбашка справа)
- Перевір що це ТЕКСТОВЕ повідомлення (не фото/відео)

---

### ТЕСТ 3: Редагування повідомлення

1. Довгий тап на **своє текстове** повідомлення
2. Натисни **"Редагувати"**

**ЩО МАЄ СТАТИСЬ:**
- 🔔 Toast: "✏️ Редагування розпочато! Текст: ..."
- 🟠 Помаранчевий індикатор з'явиться над полем вводу
- 📝 Текст повідомлення з'явиться в полі вводу
- ❌ Кнопка X для скасування

3. Зміни текст
4. Натисни **Send** (кнопка відправки)

**ЩО МАЄ СТАТИСЬ:**
- 🔔 Toast: "💾 Зберігаю зміни для повідомлення ID: ..."
- 📤 Повідомлення оновиться в чаті
- 🟠 Помаранчевий індикатор зникне

**ЯКЩО НЕ ОНОВЛЮЄТЬСЯ:**
- Перевір Logcat: `adb logcat | grep "MessagesViewModel"`
- Шукай "Edit Error" або "api_status"
- Можливо, сервер не підтримує editMessage endpoint!

---

### ТЕСТ 4: Видалення повідомлення

1. Довгий тап на **своє** повідомлення
2. Натисни **"Видалити"**

**ЩО МАЄ СТАТИСЬ:**
- 🗑️ Повідомлення зникне з чату

**ЯКЩО НЕ ВИДАЛЯЄТЬСЯ:**
- Перевір Logcat: `adb logcat | grep "deleteMessage"`
- Можливо, сервер не підтримує deleteMessage endpoint!

---

### ТЕСТ 5: Бульбашки повідомлень

Подивись на бульбашки:

**ЩО МАЄ БУТИ:**
- 📏 НЕ на весь екран (адаптивні під текст)
- 📐 Максимум 250dp ширини
- 🎨 Білі зліва (чужі), сині справа (свої)

**ЯКЩО ВСЕ ЩЕ ШИРОКІ:**
- Старий APK встановлений!
- Перевір дату модифікації APK файлу

---

### ТЕСТ 6: Аудіо плеєр

Якщо є голосові повідомлення:

**ЩО МАЄ БУТИ:**
- 🎵 Компактний плеєр (200-240dp)
- ⭕ Кругла кнопка Play
- 📊 Слайдер прогресу
- ⏱️ Час відтворення

**ЯКЩО ВИГЛЯДАЄ ІНАКШЕ:**
- Старий APK або неправильний файл MessagesScreen.kt

---

## 🐛 ДІАГНОСТИКА ПРОБЛЕМ:

### Logcat команди:

```bash
# Всі логи від додатку:
adb logcat | grep -i "worldmates\|MessagesScreen\|MessagesViewModel"

# Тільки помилки:
adb logcat *:E | grep -i "worldmates"

# API виклики:
adb logcat | grep -i "editMessage\|deleteMessage\|api_status"
```

### Якщо Toast НЕ З'ЯВЛЯЮТЬСЯ:

**Це означає що:**
- ❌ Встановлений СТАРИЙ APK
- ❌ Build не пройшов успішно
- ❌ APK з неправильної гілки

**Рішення:**
1. Перевір що на гілці `claude/fix-swipe-media-upload-Kx6yS`
2. Видали `app/build/` директорію
3. Зроби Clean + Rebuild
4. Встанови НОВИЙ APK

### Якщо Toast З'ЯВЛЯЮТЬСЯ, але функції не працюють:

**Це означає що:**
- ✅ Код виконується
- ❌ Сервер НЕ підтримує API endpoints

**Перевір Logcat для:**
- `"Edit Error: ..."` - сервер повернув помилку
- `"api_status": "404"` - endpoint не знайдений
- `"server_key not specified"` - не вистачає параметра

**Рішення:**
- Перевір чи сервер підтримує `?type=edit_message`
- Перевір чи сервер підтримує `?type=delete_message`
- Можливо, потрібно оновити серверний код!

---

## 📊 ОЧІКУВАНІ РЕЗУЛЬТАТИ:

| Функція | Toast | Результат |
|---------|-------|-----------|
| Довгий тап | "🎯 Довгий тап працює!" | Меню відкривається |
| Натискання Edit | "✏️ Редагування розпочато!" | Індикатор з'являється |
| Збереження змін | "💾 Зберігаю зміни..." | Повідомлення оновлюється |
| Бульбашки | - | Адаптивні, max 250dp |
| Аудіо плеєр | - | Компактний, 200-240dp |

---

## ✅ УСПІШНЕ ТЕСТУВАННЯ:

Якщо ВСЕ працює:
1. Toast-повідомлення з'являються ✅
2. Меню відкривається ✅
3. Edit показується для своїх повідомлень ✅
4. Редагування працює ✅
5. Видалення працює ✅
6. Бульбашки адаптивні ✅
7. Аудіо плеєр компактний ✅

**→ Код працює правильно!** 🎉

---

## ❌ ЯКЩО НЕ ПРАЦЮЄ:

Надішли мені:

1. **Screenshot Build Output** з Android Studio
2. **Screenshot встановленого APK** (дата модифікації)
3. **Screenshot тестування** (що бачиш на екрані)
4. **Logcat вивід:**
```bash
adb logcat | grep -i "worldmates\|ERROR" > logcat.txt
# Надішли logcat.txt
```

Це допоможе знайти проблему! 🔍
