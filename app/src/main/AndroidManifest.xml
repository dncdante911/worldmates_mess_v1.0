<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- ✅ Bluetooth для наушников/гарнитуры во время звонков -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- ✅ Android 14+ Foreground Service Types для звонков и видео -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- App updates: allow installing APK from within the app -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Features -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:name=".WMApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorldMatesMessenger"
        android:usesCleartextTraffic="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        tools:targetApi="31">

        <!-- Login Activity (LAUNCHER) -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register Activity -->
        <activity
            android:name=".ui.register.RegisterActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".ui.login.LoginActivity" />

        <!-- Verification Activity -->
        <activity
            android:name=".ui.verification.VerificationActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".ui.register.RegisterActivity" />

        <!-- App Lock Activity (Security) -->
        <activity
            android:name=".ui.security.AppLockActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleInstance" />

        <!-- Chats Activity -->
        <activity
            android:name=".ui.chats.ChatsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:launchMode="singleTop" />

        <!-- Messages Activity -->
        <activity
            android:name=".ui.messages.MessagesActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:windowSoftInputMode="adjustResize"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:parentActivityName=".ui.chats.ChatsActivity" />

        <!-- Groups Activity -->
        <activity
            android:name=".ui.groups.GroupsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:parentActivityName=".ui.chats.ChatsActivity" />

        <!-- Group Details Activity -->
        <activity
            android:name=".ui.groups.GroupDetailsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:parentActivityName=".ui.groups.GroupsActivity" />

        <!-- Group Admin Panel Activity -->
        <activity
            android:name=".ui.groups.GroupAdminPanelActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:parentActivityName=".ui.groups.GroupDetailsActivity" />

        <!-- Channel Details Activity -->
        <activity
            android:name=".ui.channels.ChannelDetailsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:parentActivityName=".ui.chats.ChatsActivity" />

        <!-- Channel Admin Panel Activity -->
        <activity
            android:name=".ui.channels.ChannelAdminPanelActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:parentActivityName=".ui.channels.ChannelDetailsActivity" />

        <!-- Create Channel Activity -->
        <activity
            android:name=".ui.channels.CreateChannelActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:parentActivityName=".ui.chats.ChatsActivity" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:parentActivityName=".ui.chats.ChatsActivity" />

        <!-- Theme Settings Activity -->
        <activity
            android:name=".ui.theme.ThemeSettingsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:parentActivityName=".ui.messages.MessagesActivity" />

        <!-- User Profile Activity -->
        <activity
            android:name=".ui.profile.UserProfileActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:parentActivityName=".ui.chats.ChatsActivity" />

        <!-- Call History Activity -->
        <activity
            android:name=".ui.calls.CallHistoryActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar" />

        <!-- Calls Activity -->
        <activity
            android:name=".ui.calls.CallsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:showWhenLocked="true"
            android:turnScreenOn="true" />

        <!-- Incoming Call Activity -->
        <activity
            android:name=".ui.calls.IncomingCallActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:launchMode="singleTop"
            android:excludeFromRecents="true"
            tools:ignore="MissingClass" />

        <!-- Drafts Activity -->
        <activity
            android:name=".ui.drafts.DraftsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:parentActivityName=".ui.chats.ChatsActivity" />

        <!-- Story Viewer Activity -->
        <activity
            android:name=".ui.stories.StoryViewerActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.WorldMatesMessenger.NoActionBar"
            android:parentActivityName=".ui.chats.ChatsActivity" />

        <!-- Dropbox OAuth Callback Activity -->
        <activity
            android:name="com.dropbox.core.android.AuthActivity"
            android:configChanges="orientation|keyboard"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="worldmates" android:host="dropbox-auth-callback" />
            </intent-filter>
        </activity>

        <!-- Firebase Messaging Service -->
        <service
            android:name=".services.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Music Playback Service (background audio, notification controls, lock screen) -->
        <service
            android:name=".services.MusicPlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- FileProvider for sharing files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAtat1dd1Y51O5hxB8kArGovzhUGgy7g6E" />

    </application>

    </manifest>