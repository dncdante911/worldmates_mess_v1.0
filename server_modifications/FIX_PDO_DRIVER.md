# 🔧 Виправлення проблеми "could not find driver"

## ❌ Проблема
```
❌ Помилка підключення: could not find driver
```

Це означає, що PDO драйвер MySQL не встановлений або не увімкнений на сервері.

---

## ✅ Рішення

### Крок 1: Перевір яка версія PHP використовується

```bash
php -v
```

Припустимо це PHP 8.1 (замість 8.1 підстав свою версію в командах нижче).

### Крок 2: Перевір чи встановлений модуль pdo_mysql

```bash
php -m | grep pdo_mysql
```

Якщо **нічого не виводиться** - модуль не встановлений.

### Крок 3: Встанови PDO MySQL драйвер

#### Для Debian/Ubuntu:
```bash
# Для PHP 8.1 (підстав свою версію)
sudo apt-get update
sudo apt-get install php8.1-mysql

# Або загальна команда (встановить для поточної версії PHP)
sudo apt-get install php-mysql
```

#### Для CentOS/RHEL/AlmaLinux:
```bash
# Для PHP 8.1 (підстав свою версію)
sudo yum install php81-php-mysqlnd

# Або
sudo dnf install php-mysqlnd
```

### Крок 4: Перезапусти PHP-FPM

```bash
# Дізнайся яка версія PHP-FPM запущена
systemctl list-units | grep php

# Перезапусти (підстав правильну версію)
sudo systemctl restart php8.1-fpm

# Або
sudo systemctl restart php-fpm
```

### Крок 5: Перевір що модуль завантажився

```bash
php -m | grep pdo_mysql
```

Має вивести:
```
pdo_mysql
```

### Крок 6: Перевір через phpinfo

Створи файл:
```bash
nano /var/www/www-root/data/www/worldmates.club/api/v2/info.php
```

Вміст:
```php
<?php
phpinfo();
```

Відкрий у браузері:
```
https://worldmates.club/api/v2/info.php
```

Шукай секцію **pdo_mysql** - вона має бути присутня.

**ВАЖЛИВО:** Після перевірки видали цей файл:
```bash
rm /var/www/www-root/data/www/worldmates.club/api/v2/info.php
```

### Крок 7: Протестуй підключення знову

```bash
php /var/www/www-root/data/www/worldmates.club/api/v2/test_connection.php
```

Має вивести:
```
✅ Підключення до БД успішне!
✅ Таблиця Wo_GroupChat існує. Груп в БД: X
✅ Таблиця Wo_Users існує. Активних користувачів: X
```

---

## 🔍 Альтернативне рішення: Використати mysqli замість PDO

Якщо не вдається встановити PDO драйвер, можна переписати API для використання mysqli (але це не рекомендується, PDO краще).

---

## 📝 Примітки

### ISPmanager
Якщо у тебе ISPmanager 6, можеш встановити модуль через панель:

1. Зайди в ISPmanager
2. Перейди в розділ **WWW** → **PHP настройки**
3. Обери потрібну версію PHP
4. Увімкни модуль **pdo_mysql** або **mysql**
5. Натисни **Зберегти**
6. Перезапусти PHP-FPM

### Перевірка всіх PHP модулів

```bash
php -m
```

Необхідні модулі для API:
- ✅ pdo
- ✅ pdo_mysql
- ✅ json
- ✅ mbstring

---

## 🚀 Після виправлення

1. Оновіть config.php на сервері з виправленим timezone:
```bash
nano /var/www/www-root/data/www/worldmates.club/api/v2/config.php
```

Змініть:
```php
date_default_timezone_set('Europe/Kyiv'); // Було Europe/Kiev
```

2. Протестуйте API:
```bash
php /var/www/www-root/data/www/worldmates.club/api/v2/test_connection.php
```

3. Протестуйте через браузер:
```
https://worldmates.club/api/v2/group_chat_v2.php?access_token=test
```

Має вивести JSON відповідь (навіть якщо з помилкою - головне що не PHP помилка).

---

## ✅ Готово!

Після встановлення PDO драйвера API буде працювати коректно.
